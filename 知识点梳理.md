# 量子计算与量子信息

## CHAPTER 2

#### 定义

$$
\left | \psi  \right \rangle,ket向量,列形式\\
\left \langle \psi \right | ,bra向量,行形式,\left | \psi  \right \rangle的厄米共轭\\
\langle \varphi | \psi \rangle,内积\\
|\varphi\rangle \langle \psi|,外积\\
A^*,A矩阵的复共轭或A复数的复共轭\\
A^T,A矩阵的转置\\
A^\dagger ,A矩阵的厄米矩阵/伴随\\
exp(x),等价于e^x
$$

**向量空间 $C^n$**：表示所有 $n$ 元复数组成的向量集

**向量空间生成集**：是向量集 $| v_1\rangle\sim | v_n \rangle$，使得任一向量 $|v\rangle =\sum_ia_i|v_i\rangle$。我们说向量 $|v_1\rangle\sim|v_n\rangle$ 张成了向量空间 $C^n$

**线性相关**：有 $\sum_ia_i|v_i\rangle=0$ 且存在一个 $a_i$ 非零，即 $|v_1\rangle\sim |v_n\rangle$ 线性相关，反之线性无关

**可逆的**：存在 $AB=I$，$A$ 为可逆的。当且仅当 $det(A)\neq 0$

**向量空间的基**：张成向量空间的无关组，确定向量空间后，所含元素个数为定值

**算子**：对于 $V\to W$ 的算子 $A$，$V$ 为输入空间，$W$ 为目标空间，$|v\rangle$ 为线性空间 $V$ 上的任一元素，$A|v\rangle$ 为该向量通过算子 $A$ 映射到目标空间的某一元素。这里向量空间上选择的基不同，算子对应的矩阵也可能不同。算子可以传递，即 $A:V\to W,B:W\to X,BA:V\to X$。作用为对于任意向量，通过输入基的组成来直接通过矩阵计算求得输出基的组成。

**线性算子**：对输入具有线性关系的映射，即$\left\{\begin{matrix}
Aa|v\rangle=aA|v\rangle\\
A(|v\rangle+|u\rangle)=A|v\rangle+A|u\rangle
\end{matrix}\right.$。定义在线性空间 $V$ 上的线性算子 $A$ ，指 $V\to V$

**恒等算子**：$I_v|v\rangle=|v\rangle$

**零算子**：$0|v\rangle=0$

**线性算子的矩阵表示**：需要指定一组输入基和输出基矢态。在这里算子是已经确定的，但形式很抽象，需要利用矩阵将其具体：$A|v_j\rangle=\sum_iA_{ij}|w_i\rangle$，即找出该输入 $|v_j\rangle$ 对应输出，通过以输出基对该输出进行线性分解，确定出矩阵表示中 $j$ 列的对应值

**泡利矩阵**
$$
\sigma_0=I\\
\sigma_x=\sigma_1=X=\begin{bmatrix}
 0 &1 \\
 1 &0
\end{bmatrix}\\
\sigma_y=\sigma_2=Y=\begin{bmatrix}
 0 &-i \\
 i &0
\end{bmatrix}\\
\sigma_z=\sigma_3=Z=\begin{bmatrix}
 1 &0 \\
 0 &-1
\end{bmatrix}
$$
**内积**：$\langle \varphi|\psi\rangle$，在 $C^n$ 上为 $\sum_i\varphi_i^*\psi_i$，这里$\langle\varphi|=|\varphi\rangle^\dagger$，表示一个数

**正交的**：$\langle v|w\rangle=0$，$|v\rangle$ 和 $|w\rangle$ 是正交的

**范数**：$|||v\rangle||=\sqrt{\langle v|v\rangle}$，若范数为 $1$，则称该向量为**正规化的**，是**单位向量**

**标准正交基**：基中每个向量都是单位向量且两两正交。有**格拉姆-施密特正交化方法**。后续输入输出基均应正交化
$$
|v_{k+1}\rangle=\frac{|w_{k+1}\rangle-\sum_{i=1}^k\langle v_i|w_{k+1}\rangle v_i\rangle}{|||w_{k+1}\rangle-\sum_{i=1}^k\langle v_i|w_{k+1}\rangle v_i\rangle||}
$$
**外积**：$|\varphi\rangle\langle\psi|$，因为 $ket,bra$ 向量都可以矩阵表示，所以具有结合律，可以发现，内积的值是一个数，所以可以利用内积的结合律做一些小的换位，如 $|w\rangle\langle v|u\rangle=\langle v|u\rangle |w\rangle$，可以很好地将列向量 $|u\rangle$ 转换为另一种形式的列向量 $|w\rangle$

**完备性原理**：$A:V\to W$，$A=I_WAI_V=\sum_{ij}\langle w_j| A | v_i\rangle|w_j\rangle\langle v_i|$，这个式子后面可以看做矩阵表示中 $j$ 行 $i$ 列，这里$|w_j\rangle,|v_i\rangle$ 向量是基于输入输出基的，即表示 $w$ 输出基的第 $j$ 项叠加一次

**柯西-施瓦茨不等式**：$|\langle v|w \rangle|^2\le \langle v|v\rangle \langle w|w\rangle$

**特征向量**：$|v\rangle$，使得 $A|v\rangle=v|v\rangle$，这里 $v$ 表示**特征值**，根据 $c(\lambda)=det|A-\lambda I|=0$ 求解特征值 $\lambda$，再代入 $(A-\lambda I)|v\rangle=0$ 求解对应特征向量

**特征空间**：需要先钦定特征值 $v$，以 $v$ 为特征值对应的向量集合为特征值 $v$ 对应的特征空间，如果特征空间高于一维，那么该特征空间是**退化的**

**可对角化的（标准正交分解）**：一个算子有对角表示（即**有 $n$ 个线性无关的特征向量**），$A=\sum_i\lambda_i|i\rangle\langle i|$，这里 $|i\rangle$ 表示特征值 $\lambda_i$ 对应向量 $|i\rangle$，当且仅当一个算子是正规的

**厄米共轭（伴随）**：$A^\dagger={A^*}^T$

**正规的**：$A^\dagger A=AA^\dagger$

**谱分解定理**：一个算子是正规的，当且仅当这个算子可对角化。谱分解 $M$ 出来能得到**正交矩阵 $P$ 和对角矩阵 $D$**，前者由归一化的特征向量直接组成，后者由特征值为对角线元素组成，$M=PDP^{-1}$

**厄米的（自伴算子）**：$A^\dagger=A$，是正规的

**投影**：$W$ 为 $V$ 的一个子空间，$P:V\to W=\sum_i|i\rangle\langle i|$，$P$ 是投影，是厄米的。$Q=I-P$ 是 $P$ 的**正交补**

**酉的**：$U^\dagger U=I$，是正规的，$UU^\dagger =I$

**正算子（半正定）**：对于任意非零向量 $|v\rangle$，内积 $\langle v|A|v\rangle$ 必须是非负实数。如果是正实数，则为**严格正定**。正算子一定是厄米的

**算子函数**：$A$ 为正规算子，可以定义 $f(A)=\sum_af(a)|a\rangle\langle a|$，可以以此形式定义很多算子函数，步骤为谱分解后变为上式。对于正算子的平方根，$f(\lambda_i)=\sqrt{\lambda_i}$；对于正定算子的对数，$f(\lambda_i)=ln(\lambda_i)$；对于正规算子的指数，$f(\lambda_i)=e^{\lambda_i}$。谱分解后求出对角矩阵 $D$ 和正交矩阵 $P$，$f(M)=Pf(D)P^{-1}$

**矩阵的迹**：$tr(A)=\sum_iA_{ii}$，表示矩阵的对角线元素之和（不需要变换，任何时候成立）

**对易式**：$[A,B]=AB-BA$

**对易的**：$AB=BA$

**反对易式**：$\{A,B\}=AB+BA$

**反对易的**：$AB+BA=0$

**可同时对角化**：拥有一组共同的特征向量，对于特征矩阵 $P$ 相同

**可同时对角化定理**：$A,B$ 是厄米算子，$[A,B]=0$ 成立当且仅当 $A,B$ 可同时对角化

**反对称张量**：$\epsilon _{ijk}$，当有元素 $ijk$ 重复时，$\epsilon_{ijk}=0$；否则如果 $ijk$ 的逆序对为偶数时，$\epsilon_{ijk}=1$；反之为奇数 $\epsilon_{ijk}=-1$

**克罗内克符号**：$\delta_{ij}$，当 $i=j,\delta_{ij}=1$；反之 $\delta_{ij}=0$

**极式分解**：$A$ 是向量空间 $V$ 上的线性算子，存在酉算子 $U$ 和正算子 $J,K$ 使得 $A=UJ=JK$，$J=\sqrt{A^\dagger A},K=\sqrt{AA^\dagger }$，且 $J,K$ 是唯一满足这些等式的**正算子**。如果 $A$ 可逆，$U$ 唯一

#### 推论

$$
\langle v|(\sum_i\lambda_i|w_i\rangle),=\sum_i\lambda_i\langle v|w_i\rangle\\
\langle v|w\rangle=(\langle w|v\rangle)^*\\
\langle v|v\rangle\ge 0,当且仅当|v\rangle=0,等式成立v
$$

$$
I=\sum_i|i\rangle\langle i|
$$

$$
(AB)^\dagger=B^\dagger A^\dagger\\
(AB)^{-1}=B^{-1}A^{-1}\\
det(AB)=det(A)det(B)\\
(A^\dagger)^{-1}=(A^{-1})^\dagger
$$

$$
|v_i\rangle 是一组标准正交基,U|w_i\rangle也是一组标准正交基
\\如果|v_i\rangle,|w_i\rangle是两组标准正交基,那么\sum_i|w_i\rangle\langle v_i|是酉算子
$$

$$
厄米算子H的特征值必定是实数;在\langle v|H|v\rangle下的值也是实数\\ 
对于任意算子\ A,可将其拆解为\ B+Ci,B\ 和\ C\ 都是厄米的
$$

$$
tr(AB)=tr(BA)\\
tr(uA+vB)=utr(A)+vtr(B)\\
tr(A)=tr(U^\dagger AU)\\
|\psi\rangle是单位向量，以其进行格拉姆施密特方法将其扩展到一组正交基|i\rangle 上,|\psi\rangle=|1\rangle,tr(A|\psi\rangle\langle\psi|)=\langle\psi|A|\psi\rangle\\
对于任何复数域上的方阵,矩阵的迹等于其特征值之和
$$

$$
[\sigma_j,\sigma_k]=2i\sum_{l=1}^3\epsilon_{jkl}\sigma_{l}\\
\{\sigma_j,\sigma_k\}=2\delta_{jk}I
\\2AB=[A,B]+\{A,B\}\\
[A,B]^\dagger=[B^\dagger,A^\dagger]
$$



